{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": mod_authors,
  "contact": {
    "website": "${mod_display_url}",
    "repo": "${mod_repo_url}",
    "issues": "${mod_issue_tracker_url}"
  },
  "license": "${mod_license}",
  "icon": "icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "earth.terrarium.potmeetkettle.fabric.PotMeetKettleFabric"
    ]
  },
  "depends": {
    "fabric": ">=${fabric_api_version}",
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": ">=${minecraft_version}"
  }
}
